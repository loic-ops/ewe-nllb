services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - model-cache:/root/.cache/huggingface
    environment:
      - MODEL_PATH=${MODEL_PATH:-}
    restart: unless-stopped

volumes:
  model-cache:
